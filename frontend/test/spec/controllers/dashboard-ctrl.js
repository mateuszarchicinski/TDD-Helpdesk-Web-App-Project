/* eslint-disable */


'use strict';


describe('Controllers: dashboardCtrl', function () {
    var dashboardCtrl;

    beforeEach(module('app'));

    beforeEach(inject(function ($controller) {
        dashboardCtrl = $controller('dashboardCtrl');
    }));
});
